## Role: Reviewer (agent #3)

You review the uncommitted changes in this workspace against the ticket at the
path given in your prompt.

HARD RULES:
- You MUST run the project's tests yourself. Every claim about test status
  must come from output you actually ran.
- You have NO edit or write tools. Do not ask for them. You cannot fix code.
- Never invent a reference solution ("the correct fix would use X" is
  forbidden) — none was shown to you and none exists for you.
- Every statement must cite evidence: a test name, a file:line, or captured
  command output.
- Use extremely easy words. Straightforward. Not dramatic. No unverified
  assumptions. Do not invent problems to seem thorough.
- CONTEXT BUDGET: never read large or generated artifacts (notebooks .ipynb,
  images, PDFs, archives, binaries) or any file above ~30KB — check sizes
  first with ls -la, then grep for what you need. Use targeted reads, grep, and
  test output instead of dumping whole files or whole listings.

Your deliverable is ONE file: the feedback report at the exact path given in
your prompt. It MUST use exactly these sections (markdown headers, order):
# Feedback report
## Tests
## What is missing
## Why it fails
## Verdict
## Experience difference

Section rules:
- Tests: the total number of tests, how many passed, how many failed, with the
  command you ran and its real output excerpt. If no tests ran, say so
  explicitly.
- What is missing: what the ticket asked for that is not implemented —
  product-wise AND user-experience-wise.
- Why it fails: the exact reason of every failing test.
- Verdict: one line, exactly `VERDICT: approve` or `VERDICT: changes_needed`.
- Experience difference: the comprehensive difference between the product the
  ticket describes and the product that actually exists now. Cover every
  meaningful aspect of the user experience, not a summary line.
- NO implementation code. NO how-to-fix plans.


## Plan for the feedback report (review of staged changes vs `/megabit/ticket.md`)

Evidence gathered below is from commands I actually ran in this environment.

---

### `# Feedback report`
One-line context: changes = `rich/markdown.py` (+24/−4), new `tests/test_markdown_inline_code_table.py` (+92), `docs/source/markdown.rst` (+25), `CHANGELOG.md` (+1). Ticket = colored inline code (`inline_code_lexer`) must keep its colors inside markdown table cells (heading row + body rows), nothing else in tables may change.

### `## Tests` — what to state
- Command: `python -m pytest tests -q` → **`961 passed, 24 skipped, 1 warning in 5.67s`** (0 failures). Warning is pre-existing (`PytestRemovedIn10Warning` in `test_panel.py` parametrize), unrelated.
- Focus run: `python -m pytest tests/test_markdown_inline_code_table.py tests/test_markdown.py -v` → `12 passed` (5 new + 7 markdown tests incl. `test_markdown_table`, `test_inline_styles_in_table`, `test_inline_styles_with_justification`, `test_partial_table`, `test_table_with_empty_cells`).
- Baseline count: `python -m pytest tests -q --ignore=tests/test_markdown_inline_code_table.py` → `956 passed, 24 skipped` → the change adds exactly 5 tests.
- Proof the new tests are real regression tests: copied `rich/` at HEAD (`git show HEAD:rich/markdown.py`) to `/tmp/origrepo` with the new test file and ran it there → **`3 failed, 2 passed`**:
  - `test_inline_code_in_table_body_cell` / `test_inline_code_in_table_heading_row`: expected `38;2;248;248;242;48;2;39;40;34mprint…`, got flat `36;40mprint("hi", 1)`.
  - `test_inline_code_in_table_matches_paragraph:69`: `AssertionError: assert [] == [('38;2;248;248;242;48;2;39;40;34', 'print'), …]` (body cell had zero code-theme runs).
- No-lexer claim verified end-to-end via CLI: old `python -m rich.markdown /tmp/t.md -i python -c` → `^[[1;36;40mprint^[[0m…` (one flat color); new → `^[[97;40mprint^[[0m^[[93;40m"^[[0m…` (per-token colors).
- Static checks: cannot verify — `mypy: command not found`, `black: command not found` (project declares `strict = true`, `pyproject.toml:52-56`). Import/execution of the touched module works.

### `## What is missing` — points to make
Nothing in the ticket's behaviour list is unimplemented (verified below). What is missing is test/consistency quality:
1. No committed test for offset maths: every new test puts the snippet alone in the cell. Untested by the file: plain text before/after code in the same header cell, two snippets in one cell (`Span(offset + start, …)`, `rich/markdown.py:345-351`). I probed these by hand and they render correctly today, so this is a coverage gap, not a product gap.
2. No test for wrapped multi-line code cells or for `inline_code_theme`/`code_theme` combinations in tables (hand-checked: monokai/native/solarized-light/fruity all give cell runs identical to paragraph runs).
3. The new file re-declares its own `render()` (`tests/test_markdown_inline_code_table.py:21-26`) instead of using `tests/render.py`, and so skips `replace_link_ids` (`tests/render.py:6-16`, used at `tests/test_markdown.py:94`). Harmless today (no links in these tests), but a link/`\x1b]8` case added here would flake.
4. Ticket hint named `tests/test_markdown.py`; tests went to a new file — deviation from hint only.
5. Docs placement: `docs/source/markdown.rst:48` ("You can also use the Markdown class from the command line…") now sits inside the new "Inline code" section (only two heading levels exist in the file: `Markdown` =, `Inline code` -), and `docs/source/markdown.rst:45` shows `inline_code_theme="monokai"`, which is already the default and has no CLI flag.
6. No user-facing test/guard for the code comment's assumption at `rich/markdown.py:341-343` ("Only syntax highlighted inline code arrives as a Text object"). It is true today: the only `Text`-passing site is `rich/markdown.py:507-511` (`{"fence","code_inline"}`), and all other `on_text` calls pass `str` (lines 621-625, 650-659, 677).

### `## Why it fails`
Say plainly: **no test fails.** 0 failed in the suite (`961 passed, 24 skipped`). The only failures ever observed were the 3 in the isolated pre-fix copy (`/tmp/origrepo`), which prove the bug, not a current failure. Also state that mypy/black could not be run (not installed), so type/format status is unknown, not passing.

### `## Verdict`
`VERDICT: approve`
Reasoning to include in one line: ticket behaviour is implemented and verified, full suite green, no regressions found, remaining items are test-coverage and docs-nit picks.

### `## Experience difference` — comprehensive list to write
- **Body cells, with a lexer:** were flat `1;36;40` (bold cyan on black, `markdown.code`) for the entire snippet → now per-token pygments fg + theme bg, byte-identical to the same snippet in a sentence (verified by `test_inline_code_in_table_matches_paragraph` and my runs for monokai/emacs/native/solarized-light/fruity).
- **Heading row, with a lexer:** same change; `markdown.table.header` no longer flattens the code range because the code spans are re-applied after the header style (`rich/markdown.py:262-266`). Plain header words still render `36` cyan and padding runs unchanged.
- **Mixed cells:** `Name \`print("a")\` x \`1+1\` tail` in a header cell now shows cyan for plain words and colored runs for both snippets, in the right places (offsets correct); before, all of it was one cyan/black block.
- **Multiple snippets, hard breaks (`<br>`), right/center alignment, unicode cells, narrow widths:** colors survive, including across a wrapped snippet split over several lines (verified at width 20: `def`, `very_long_functio…`, `beta`, `delta=1` each keep their own color).
- **No lexer (`Markdown(doc)`):** nothing changes — `| \`print("hi", 1)\` |` still `1;36;40` in body rows and `36;40` in headers (`test_inline_code_in_table_without_lexer_is_unchanged`).
- **Whole-product regression sweep:** rendered 39 repo `.md` files × 2 widths × {no lexer, python lexer} with old vs new code, after normalizing random hyperlink ids: **0 differences** (0 of 144). Plus 17 synthetic documents × 2 widths × 4 option sets = 136 renders: **78 differ, and every difference is in a document that has a table cell with back-ticked code AND a lexer**; all 34 no-lexer renders and every non-code run in the lexer renders are byte-identical (only code runs change, and the old bold `1;` is never added where the paragraph doesn't add it).
- **CLI (`rich-markdown` / `python -m rich.markdown`):** `argparse` untouched — `-h` output identical between HEAD and the working tree; same flags (`-c`, `-t`, `-i`, `-y`, `-w`, `-j`, `-p`); colors in cells visible with `-i python -c`.
- **Settings/API surface:** no new option, none removed; `Markdown(..., inline_code_lexer, inline_code_theme, code_theme)` behaves as before, `inline_code_theme or code_theme` still holds in tables (`rich/markdown.py:582`).
- **Jupyter / repeated renders:** `TableElement.__rich_console__` styles a copy of the cell text (`rich/markdown.py:259-267`), so printing the same `Markdown` twice is identical (checked: `idempotent: True`).
- **Documentation experience:** a reader now finds an "Inline code" section in `docs/source/markdown.rst` explaining the lexer, the theme defaults, and explicitly that snippets keep colors in table heading and body cells — previously invisible.
- **Intentional trade-off to name:** because header code spans now beat `markdown.table.header`, a custom theme that recolors header text will no longer recolor highlighted snippets in header cells (it still colors non-code header words, and still colors code when no lexer is given). This is what the ticket asked for ("colors survive"), but it is a visible rule change for themed tables.
- **Failure/edge experience:** empty cells, partial tables, and tables with no code are unchanged; control-character behaviour in cells is unchanged in practice (old and new render `| be\x07llo | x\x01y |` identically), even though the str branch switched from `Text()`+`stylize`+`append_text` to `append(text, style)` (`rich/markdown.py:339-343`).